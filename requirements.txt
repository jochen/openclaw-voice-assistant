# Core
pyaudio==0.2.14
numpy==1.26.4
scipy==1.17.1
webrtcvad-wheels==2.0.14
PyYAML==6.0.3
requests==2.32.5

# Wake word detection
openwakeword==0.6.0
tflite-runtime==2.14.0
onnxruntime==1.24.3

# STT fallback (local)
faster-whisper==1.2.1
ctranslate2==4.7.1

# TTS fallback (local) — piper-CLI im Projekt-venv; pathvalidate wird vom
# piper-CLI importiert (ist sonst nur ein train-Extra von piper-tts)
piper-tts==1.4.1
pathvalidate==3.3.1

# Verbalisierung (Zahlen, Ordinalia, Uhrzeiten in TTS-Text)
num2words>=0.5.14

# ReSpeaker — ESPHome Native API Client (nur für mode: respeaker)
aioesphomeapi>=44.16.1
